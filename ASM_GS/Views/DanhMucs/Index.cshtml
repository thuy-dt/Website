@model IEnumerable<ASM_GS.Models.DanhMuc>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenDanhMuc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDanhMuc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaDanhMuc">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaDanhMuc">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaDanhMuc">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
